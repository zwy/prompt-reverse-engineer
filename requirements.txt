# Core dependencies
# Minimum version constraints — for reproducible environments run:
#   pip freeze > requirements-lock.txt
dspy-ai>=2.5.0
openai>=1.30.0
requests>=2.31.0
pydantic>=2.0.0
python-dotenv>=1.0.0
